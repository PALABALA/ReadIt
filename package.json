{
  "name": "readit",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "daily-email": "sls invoke local -f sendDailyEmail",
    "biweekly-email": "sls invoke local -f sendBiweeklyEmail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.556.0",
    "node-fetch": "^2.6.0"
  }
}
